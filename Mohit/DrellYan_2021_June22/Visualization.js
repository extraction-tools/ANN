function generateColumn(numberOfRows, columnLength, containerName) {
	container = document.getElementById(containerName);	
	for(let c = 0; c < columnLength; c++){
		let cell = document.createElement()
	}
}